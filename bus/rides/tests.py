
    # Ajoutez d'autres tests selon vos besoins
    # tester des méthodes additionnelles dans le modèle
    # La page trip Renvoie la statut 200 si item  n'exists pas
    # La page trip renvoie la statut 400 si item exists deja
    # Reservation
    # Tester quand une requête est faite une nouvelle reservation est ajouté dans la DB
    # Tester si la reservation est bien rélié au trajet
    # Tester si le nombre de places que le client souhaite reserver ne depasse pas le nbre de places dispo
    # Tester si quand le nbre de place que le client souhaite reserver est superieur ou égale à zero
    # Tester si quand le client reserver de place ça est déduit du nbre de places dispo
